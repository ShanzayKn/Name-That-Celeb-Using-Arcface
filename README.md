# Name That Celeb – ArcFace

This project detects and recognizes celebrities from videos using **ArcFace embeddings** and a ResNet backbone.

## Folders
- `data/` → training & testing images (not included here).
- `recognizedvideos/` → contains 3 sample output videos with recognized celebrities.

## Setup
```bash
pip install -r requirements.txt
